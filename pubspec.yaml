name: fitstart_app
description: "FitStart — мобильное приложение фитнес-клуба (верстка)."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  salomon_bottom_bar: ^3.3.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - images/fitstart_logo.png
    - images/fitstart_icon.png
    - images/fitstart_logo_dark.png
